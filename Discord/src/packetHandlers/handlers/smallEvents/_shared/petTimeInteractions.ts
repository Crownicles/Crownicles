/**
 * Interaction names that involve time gain/loss for pets, used by both the
 * pet and interactOtherPlayers small event handlers.
 */
export const PET_TIME_INTERACTIONS = new Set([
	"gainTime",
	"loseTime"
]);
