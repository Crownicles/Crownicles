# Address every client uses to reach Keycloak. It fixes the issuer of the tokens and the redirect
# URI Keycloak sends to the identity providers, so it must be reachable by the phones and the
# services alike. Copy this file to .env and adjust it when localhost is not enough.
KEYCLOAK_HOSTNAME=http://localhost:8080
