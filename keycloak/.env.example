# Address every client uses to reach Keycloak. It fixes the token issuer and the redirect URI sent
# to Discord. Use the stable Mac hostname for physical iOS/Android devices, never the current LAN
# IP. Copy this file to .env and set the hostname once for this development machine.
KEYCLOAK_HOSTNAME=http://localhost:8080
