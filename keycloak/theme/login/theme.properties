parent=keycloak
import=common/keycloak

# The parent's own sheet is kept first: crownicles.css only overrides what makes the page look
# like a website rather than like the app.
styles=css/login.css css/crownicles.css
